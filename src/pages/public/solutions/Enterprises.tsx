import { AudienceTemplate } from "@/components/landing/AudienceTemplate";

export default function Enterprises() {
  return (
    <AudienceTemplate 
      metaHeadline="GLOBAL STANDARDIZATION"
      missionHeading="Enterprises"
      missionDescription="We are on a mission to help ensure every global enterprise can centralize their talent pipelines without sacrificing local compliance."
    />
  );
}
