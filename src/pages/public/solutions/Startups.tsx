import { AudienceTemplate } from "@/components/landing/AudienceTemplate";

export default function Startups() {
  return (
    <AudienceTemplate 
      metaHeadline="SCALING OPERATIONS"
      missionHeading="Startups"
      missionDescription="We are on a mission to help ensure every emerging startup scales their engineering capacity with the perfect remote talent response."
    />
  );
}
